#include "holberton.h"

/**
 * print_chessboard - prints a chessboard
 * @a: pointer to array
 *
 * Return: void
 */
void print_chessboard(char (*a)[8])
{
	int a1, a2;

	for (a1 = 0 ; a1 < 8 ; a1++)
	{
		for (a2 = 0 ; a2 < 8 ; a2++)
		{
			_putchar(a[a1][a2]);
		}
		_putchar('\n');
	}
}
