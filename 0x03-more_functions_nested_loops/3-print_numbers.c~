#include "holberton.h"

/**
 * print_numbers - prints numbers from 0 to 9 then \n
 * @num: number for output
 *
 * Return: (0) Always success
 */
void print_numbers(void)
{
	int ch;

	for (ch = '0'; ch <= '9'; ch++)
	{
		_putchar(ch);
	}
	return (0);
}
